---
trigger: always_on
---

**版本更新**：
每次对话完成后，如果有代码变更：
1. 修改 `App.tsx` 中顶部的 `CHANGELOG` 数组：添加一个新对象（包含版本号、日期、中文修改项）到数组首位。
2. 同步更新 `App.tsx` 中 Header 部分显示的最新版本号文本。
3. 版本号格式：`v26.MMDD.HHmm`（例如 `v26.0214.1135` 代表 2026年2月14日 11:35）。